{
  "name": "figma-selection-mcp-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin that sends selection data to VS Code via WebSocket",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "typescript": "^5.7.0"
  }
}